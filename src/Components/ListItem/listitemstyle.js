import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    libox: {
        marginTop: 10,
        flexDirection: 'row',
    },

    liboxicon: {
        color: '#939393',
    },

    liboxtext: {
        flex: 1,
        paddingLeft: 5,
        color: '#939393',

    },

});

export default styles;