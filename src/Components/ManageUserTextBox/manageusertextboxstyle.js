import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    manageusertextbox: {
        marginBottom: 10,
    },

    manageusertextblack: {
        color: '#000000',
        fontWeight: 'bold',
        fontSize: 12,
        marginBottom: 3,
    },

    manageusertextgray: {
        color: '#939393',
        fontWeight: 'normal',
        fontSize: 12,
    },

});

export default styles;