import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    transactiondetailhead: {
        paddingHorizontal: 8,
    },

    transactiondetailtext: {
        fontWeight: 'bold',
        fontSize: 14,
    },

    transactiondetailtextbox: {
        flexDirection: 'row',
        marginTop: 15,
    },

});

export default styles;